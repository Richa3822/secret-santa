export interface EmployeeCsvRow {
  Employee_Name: string;
  Employee_EmailID: string;
}

export interface PreviousAssignmentCsvRow {
  Employee_Name: string;
  Employee_EmailID: string;
  Secret_Child_Name: string;
  Secret_Child_EmailID: string;
}